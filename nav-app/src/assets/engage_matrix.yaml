---

id: ENGAGE
name: Engage Navigator
version: 1.0.0

tactics:
  - "{{prevent.id}}"
  - "{{direct.id}}"
  - "{{disrupt.id}}"
 

data:
  - !include tactics.yaml
  - !include techniques.yaml
  - !include case-studies/*.yaml
