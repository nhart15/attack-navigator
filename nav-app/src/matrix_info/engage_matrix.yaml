---

id: ENGAGE
name: Engage Navigator
version: 1.0.0

approaches:
  - "{{prevent.id}}"
  - "{{direct.id}}"
  - "{{disrupt.id}}"
 
data:
  - !include engage_approaches.yaml
  - !include engage_activities.yaml
