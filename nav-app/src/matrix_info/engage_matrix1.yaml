---

id: ENGAGE
name: Engage Navigator
version: 1.0.0

tactics:
  - "{{plan.id}}"
  - "{{collect.id}}"
  - "{{detect.id}}"
  - "{{prevent.id}}"
  - "{{direct.id}}"
  - "{{disrupt.id}}"
  - "{{reassure.id}}"
  - "{{motivate.id}}"
  - "{{analyze.id}}"
 
data:
  - !include engage_tactics.yaml
  - !include engage_techniques.yaml
